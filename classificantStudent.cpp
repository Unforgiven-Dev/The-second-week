#include <iostream>

using namespace std;

int main(){
    // Toi ưu hoa doc/ghi du lieu
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // Khai bao dinh dang bien
    int n;

    // Vong lap xet diem so thuc
    for (int i = 0; i < n; i++){

        //Khai bao dinh dang bien diem so cua hoc sinh
        double a;
        // Doc du lieu tu dau vao
        if (cin >> a){
            if (n >= 10 )
        }
    }

}